[package]
name = "sheetview"
version = "0.1.0"
description = "Offline CSV, Excel and HanCell viewer"
authors = ["Sheetview contributors"]
edition = "2021"
license = "MIT"

[lib]
name = "sheetview_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
